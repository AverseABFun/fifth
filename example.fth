5 % adds 5 to the stack
2
* % multiplies the top two values on the stack
# % outputs the literal number to the terminal
10 % newline char
$ % outputs the char code on the top of the stack as a character
5
3
+ % adds the top two values on the stack
#
10
$
104 % letter 'h' char
$
105 % letter 'i' char
$